import React from "react";

function EventTile(events) {
  return (
    <div>
      <h1>not working</h1>
      {/* {events &&
        events.map((event) => (
          <div>
            <h1>yo</h1>
            <div key={event._id}>
              <h3>{event.title}</h3>
            </div>
          </div>
        ))} */}
    </div>
  );
}

export default EventTile;
